vmhosts:
  hosts:
    # Example to setup the localhost
    localhost:
      ansible_connection: local # local connection
      bridge_name: br0
      bridge_int: eth0 # physical interface
      bridge_ip: 10.10.0.101
      bridge_netmask: 255.255.0.0
      bridge_gw: 10.10.0.1
      dns_server: 10.10.0.1
